# ANIDOG 番剧自动下载管理系统 - 开发规范文档

## 1. 代码规范

### 1.1 Python后端规范

#### 1.1.1 命名规范
```python
# 模块名：小写字母，单词间用下划线分隔
anime_service.py

# 类名：大驼峰命名法
class AnimeDownloader:
    pass

# 函数名和变量名：小写字母，单词间用下划线分隔
def get_anime_info(anime_id: int) -> dict:
    current_episode = 12
    return {"episode": current_episode}

# 常量：全大写，单词间用下划线分隔
MAX_RETRY_COUNT = 3
DEFAULT_TIMEOUT = 30
```

#### 1.1.2 类型注解规范
```python
from typing import List, Optional, Dict, Union
from datetime import datetime

# 函数参数和返回值必须添加类型注解
def process_anime_data(
    anime_id: int,
    title: str,
    episodes: List[int],
    metadata: Optional[Dict[str, str]] = None
) -> Dict[str, Union[str, int, List[int]]]:
    """处理番剧数据"""
    return {
        "id": anime_id,
        "title": title,
        "episodes": episodes,
        "metadata": metadata or {}
    }
```

#### 1.1.3 文档字符串规范
```python
def download_anime(anime_id: int, episode: int, quality: str = "1080p") -> bool:
    """
    下载指定番剧的某一集
    
    Args:
        anime_id: 番剧ID
        episode: 集数
        quality: 画质，可选值: 720p, 1080p, 4K
        
    Returns:
        bool: 下载是否成功
        
    Raises:
        AnimeNotFoundError: 番剧不存在
        DownloadError: 下载失败
        
    Example:
        >>> download_anime(1, 12, "1080p")
        True
    """
    pass
```

#### 1.1.4 异常处理规范
```python
# 自定义异常类
class AnimeError(Exception):
    """番剧相关异常基类"""
    pass

class AnimeNotFoundError(AnimeError):
    """番剧未找到异常"""
    pass

# 异常处理示例
try:
    anime = get_anime_by_id(anime_id)
except AnimeNotFoundError:
    logger.error(f"番剧不存在: {anime_id}")
    raise HTTPException(status_code=404, detail="番剧不存在")
except Exception as e:
    logger.exception("获取番剧信息失败")
    raise HTTPException(status_code=500, detail="服务器内部错误")
```

#### 1.1.5 代码组织规范
```python
# 导入顺序：标准库 -> 第三方库 -> 本地模块
import os
import sys
from datetime import datetime
from typing import List, Optional

import fastapi
from sqlmodel import Session, select
from loguru import logger

from app.core.config import settings
from app.models.anime import Anime
from app.services.downloader import DownloadService
```

### 1.2 Vue前端规范

#### 1.2.1 组件命名规范
```vue
<!-- 组件文件名：大驼峰命名 -->
<!-- AnimeCard.vue -->

<!-- 组件注册名：大驼峰 -->
<script setup lang="ts">
import AnimeCard from './AnimeCard.vue'
import DownloadProgress from './DownloadProgress.vue'
</script>

<!-- 模板中使用：短横线分隔 -->
<template>
  <anime-card :anime="animeData" />
  <download-progress :progress="downloadProgress" />
</template>
```

#### 1.2.2 组件结构规范
```vue
<template>
  <!-- 模板内容 -->
</template>

<script setup lang="ts">
// 1. 导入语句
import { ref, computed, onMounted } from 'vue'
import type { Anime } from '@/types/anime'

// 2. Props定义
interface Props {
  anime: Anime
  showDetails?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  showDetails: false
})

// 3. Emits定义
const emit = defineEmits<{
  select: [id: number]
  update: [anime: Anime]
}>()

// 4. 响应式数据
const isLoading = ref(false)
const selectedEpisode = ref(1)

// 5. 计算属性
const displayTitle = computed(() => {
  return props.anime.originalTitle || props.anime.title
})

// 6. 方法
const handleSelect = () => {
  emit('select', props.anime.id)
}

// 7. 生命周期
onMounted(() => {
  // 初始化逻辑
})
</script>

<style scoped>
/* 样式内容 */
</style>
```

#### 1.2.3 TypeScript规范
```typescript
// 接口定义
interface Anime {
  id: number
  title: string
  originalTitle?: string
  status: 'ongoing' | 'finished' | 'upcoming'
  episodes: Episode[]
}

// 类型别名
type AnimeStatus = 'ongoing' | 'finished' | 'upcoming'

// 函数类型定义
type FilterFunction = (anime: Anime) => boolean

// 泛型使用
function findAnime<T extends Anime>(
  animes: T[],
  predicate: (anime: T) => boolean
): T | undefined {
  return animes.find(predicate)
}

// 严格的null检查
function getAnimeTitle(anime: Anime | null): string {
  return anime?.title ?? '未知番剧'
}
```

#### 1.2.4 组合式API使用规范
```typescript
// composables/useAnime.ts
import { ref, computed, watch } from 'vue'
import type { Anime } from '@/types/anime'
import { animeApi } from '@/api/anime'

export function useAnime(animeId: number) {
  // 状态
  const anime = ref<Anime | null>(null)
  const loading = ref(false)
  const error = ref<string | null>(null)

  // 计算属性
  const isCompleted = computed(() => {
    return anime.value?.status === 'finished'
  })

  // 方法
  const fetchAnime = async () => {
    loading.value = true
    error.value = null
    
    try {
      const data = await animeApi.getAnime(animeId)
      anime.value = data
    } catch (e) {
      error.value = '获取番剧信息失败'
      console.error(e)
    } finally {
      loading.value = false
    }
  }

  // 监听器
  watch(() => animeId, (newId) => {
    if (newId) {
      fetchAnime()
    }
  })

  return {
    anime,
    loading,
    error,
    isCompleted,
    fetchAnime
  }
}
```

### 1.3 样式规范

#### 1.3.1 CSS类命名规范（BEM）
```css
/* Block */
.anime-card { }

/* Element */
.anime-card__title { }
.anime-card__cover { }
.anime-card__actions { }

/* Modifier */
.anime-card--featured { }
.anime-card--disabled { }

/* 状态类 */
.is-active { }
.is-loading { }
.has-error { }
```

#### 1.3.2 Tailwind CSS使用规范
```vue
<template>
  <!-- 使用语义化的类组合 -->
  <div class="anime-grid">
    <div class="anime-card-wrapper">
      <img class="anime-cover" :src="coverUrl" />
      <h3 class="anime-title">{{ title }}</h3>
    </div>
  </div>
</template>

<style scoped>
/* 将常用的Tailwind类组合成语义化的类 */
.anime-grid {
  @apply grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6;
}

.anime-card-wrapper {
  @apply bg-white dark:bg-gray-800 rounded-lg shadow-md hover:shadow-lg transition-shadow;
}

.anime-cover {
  @apply w-full h-48 object-cover rounded-t-lg;
}

.anime-title {
  @apply text-lg font-semibold text-gray-900 dark:text-white p-4;
}
</style>
```

## 2. Git规范

### 2.1 分支管理
```bash
# 主分支
main              # 生产环境代码
develop           # 开发环境代码

# 功能分支
feature/rss-filter       # 新功能开发
feature/ui-redesign      # UI重构

# 修复分支
bugfix/download-failed   # 常规bug修复
hotfix/security-patch    # 紧急修复

# 发布分支
release/v1.2.0          # 版本发布准备
```

### 2.2 提交信息规范
```bash
# 格式：<type>(<scope>): <subject>

# 类型说明
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整（不影响功能）
refactor: 代码重构
perf: 性能优化
test: 测试相关
chore: 构建过程或辅助工具的变动

# 示例
feat(anime): 添加番剧批量下载功能
fix(rss): 修复RSS解析中文乱码问题
docs(api): 更新API文档
style(frontend): 统一代码缩进格式
refactor(auth): 重构认证模块
perf(database): 优化查询性能
test(download): 添加下载模块单元测试
chore(deps): 升级依赖包版本
```

### 2.3 代码审查规范
- PR标题清晰描述改动内容
- PR描述包含：背景、改动说明、测试方法
- 代码必须通过CI检查
- 至少一人审查通过
- 解决所有review意见

## 3. 测试规范

### 3.1 后端测试规范
```python
# test_anime_service.py
import pytest
from app.services.anime import AnimeService
from app.models.anime import Anime

class TestAnimeService:
    """番剧服务测试类"""
    
    @pytest.fixture
    def anime_service(self):
        """创建测试用的服务实例"""
        return AnimeService()
    
    @pytest.fixture
    def sample_anime(self):
        """创建测试用的番剧数据"""
        return Anime(
            title="测试番剧",
            status="ongoing",
            episode_count=12
        )
    
    def test_create_anime_success(self, anime_service, sample_anime):
        """测试创建番剧成功的场景"""
        # Given
        anime_data = sample_anime
        
        # When
        result = anime_service.create_anime(anime_data)
        
        # Then
        assert result.id is not None
        assert result.title == "测试番剧"
        
    def test_create_anime_duplicate(self, anime_service, sample_anime):
        """测试创建重复番剧的场景"""
        # Given
        anime_service.create_anime(sample_anime)
        
        # When & Then
        with pytest.raises(DuplicateAnimeError):
            anime_service.create_anime(sample_anime)
```

### 3.2 前端测试规范
```typescript
// AnimeCard.test.ts
import { describe, it, expect, vi } from 'vitest'
import { mount } from '@vue/test-utils'
import AnimeCard from '@/components/AnimeCard.vue'

describe('AnimeCard', () => {
  const mockAnime = {
    id: 1,
    title: '测试番剧',
    status: 'ongoing',
    currentEpisode: 5,
    totalEpisodes: 12
  }

  it('应该正确渲染番剧信息', () => {
    const wrapper = mount(AnimeCard, {
      props: {
        anime: mockAnime
      }
    })

    expect(wrapper.find('.anime-title').text()).toBe('测试番剧')
    expect(wrapper.find('.anime-progress').text()).toContain('5/12')
  })

  it('点击时应该触发select事件', async () => {
    const wrapper = mount(AnimeCard, {
      props: {
        anime: mockAnime
      }
    })

    await wrapper.trigger('click')
    
    expect(wrapper.emitted()).toHaveProperty('select')
    expect(wrapper.emitted('select')?.[0]).toEqual([mockAnime.id])
  })
})
```

## 4. 文档规范

### 4.1 README规范
```markdown
# 项目名称

简短的项目描述

## 特性

- 特性1
- 特性2
- 特性3

## 快速开始

### 环境要求

- Python 3.11+
- Node.js 18+
- Docker 20+

### 安装步骤

1. 克隆项目
2. 安装依赖
3. 配置环境变量
4. 启动服务

## 使用说明

### 基本用法

示例代码

### 高级配置

配置说明

## 开发指南

### 项目结构

目录说明

### 贡献指南

如何贡献代码

## 许可证

MIT License
```

### 4.2 API文档规范
```python
@router.post("/animes", response_model=AnimeResponse)
async def create_anime(
    anime_data: AnimeCreate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user)
):
    """
    创建新的番剧记录
    
    **权限要求**: 需要登录
    
    **请求参数**:
    - title: 番剧标题（必填）
    - originalTitle: 原始标题（可选）
    - status: 番剧状态 (ongoing/finished/upcoming)
    - season: 季度
    - year: 年份
    
    **响应说明**:
    - 200: 创建成功，返回番剧信息
    - 400: 请求参数错误
    - 401: 未授权
    - 409: 番剧已存在
    
    **示例请求**:
    ```json
    {
        "title": "某科学的超电磁炮",
        "originalTitle": "とある科学の超電磁砲",
        "status": "ongoing",
        "season": 3,
        "year": 2025
    }
    ```
    """
    # 实现代码
```

## 5. 安全规范

### 5.1 密码处理
```python
# 密码必须使用bcrypt加密
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def hash_password(password: str) -> str:
    """加密密码"""
    return pwd_context.hash(password)

def verify_password(plain_password: str, hashed_password: str) -> bool:
    """验证密码"""
    return pwd_context.verify(plain_password, hashed_password)
```

### 5.2 环境变量
```python
# 敏感信息必须通过环境变量配置
# .env.example
DATABASE_URL=sqlite:///./anidog.db
SECRET_KEY=your-secret-key-here
QBITTORRENT_USERNAME=admin
QBITTORRENT_PASSWORD=adminpass

# 生产环境不要提交.env文件
# .gitignore
.env
.env.local
.env.production
```

### 5.3 输入验证
```python
# 使用Pydantic进行输入验证
from pydantic import BaseModel, EmailStr, validator

class UserCreate(BaseModel):
    username: str
    email: EmailStr
    password: str
    
    @validator('username')
    def username_valid(cls, v):
        if not 3 <= len(v) <= 20:
            raise ValueError('用户名长度必须在3-20个字符之间')
        if not v.isalnum():
            raise ValueError('用户名只能包含字母和数字')
        return v
    
    @validator('password')
    def password_strong(cls, v):
        if len(v) < 8:
            raise ValueError('密码长度至少8个字符')
        return v
```

## 6. 性能规范

### 6.1 数据库查询优化
```python
# 避免N+1查询问题
# 错误示例
animes = db.query(Anime).all()
for anime in animes:
    episodes = db.query(Episode).filter(Episode.anime_id == anime.id).all()

# 正确示例
from sqlalchemy.orm import joinedload

animes = db.query(Anime).options(joinedload(Anime.episodes)).all()
```

### 6.2 前端性能优化
```vue
<!-- 使用v-show替代v-if进行频繁切换 -->
<div v-show="isVisible">内容</div>

<!-- 大列表使用虚拟滚动 -->
<virtual-list
  :data="animeList"
  :item-height="200"
  :overscan="5"
>
  <template #default="{ item }">
    <anime-card :anime="item" />
  </template>
</virtual-list>

<!-- 图片懒加载 -->
<img v-lazy="coverUrl" alt="封面">
```

## 7. 日志规范

### 7.1 日志级别使用
```python
from loguru import logger

# DEBUG: 调试信息
logger.debug(f"开始处理番剧: {anime_id}")

# INFO: 一般信息
logger.info(f"番剧下载完成: {anime_title}")

# WARNING: 警告信息
logger.warning(f"RSS源响应缓慢: {rss_url}")

# ERROR: 错误信息
logger.error(f"下载失败: {error_message}")

# CRITICAL: 严重错误
logger.critical("数据库连接失败，系统无法启动")
```

### 7.2 日志格式
```python
# 配置日志格式
logger.add(
    "logs/app_{time:YYYY-MM-DD}.log",
    rotation="00:00",
    retention="30 days",
    format="{time:YYYY-MM-DD HH:mm:ss} | {level} | {module}:{function}:{line} - {message}",
    level="INFO"
)
```

## 8. 部署规范

### 8.1 环境分离
```bash
# 开发环境
.env.development

# 测试环境
.env.testing

# 生产环境
.env.production
```

### 8.2 健康检查
```python
@app.get("/health")
async def health_check():
    """健康检查接口"""
    return {
        "status": "healthy",
        "timestamp": datetime.utcnow(),
        "version": settings.VERSION,
        "services": {
            "database": check_database_connection(),
            "redis": check_redis_connection(),
            "qbittorrent": check_qbittorrent_connection()
        }
    }
```

---

文档版本：1.0  
创建日期：2025-01-03  
作者：技术团队