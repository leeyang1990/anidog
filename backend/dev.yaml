access_token_expire_minutes: 1440

# 内嵌 BT 引擎（开发与生产使用同一实现）
downloader_type: "embedded"
bt_listen_port: 6881
bt_state_dir: "/var/lib/anidog/torrent"
bt_seed: true

# 数据库（Docker 开发环境）
database_url: "postgres://anidog:anidog123@postgres:5432/anidog_dev?sslmode=disable"

# 数据库连接池
db_max_open_conns: 25
db_max_idle_conns: 5

# 默认规则
enable_default_rules: true
default_rule_name: "mikan"

# 日志
log_level: "DEBUG"

# RSS
rss_check_interval: 30

# 流媒体
stream_max_concurrent: 3
stream_min_duration_seconds: 100
rod_headless: true
stream_intercept_timeout: 30

# TMDB
tmdb_language: "zh-CN"
tmdb_base_url: "https://api.themoviedb.org/3"

# 通知
enable_notifications: false

# JWT 密钥（生产环境请通过 SECRET_KEY env 覆盖）
secret_key: "devsecretkey"
